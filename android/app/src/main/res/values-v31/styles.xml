<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31) always draws a system splash screen built from the
     launch theme. Without this override the system would show the app icon on
     a white background, which is exactly the "default splash" we removed.

     Here the system splash is reduced to the Mr. Cake brand colour with a
     transparent icon, so the user only ever sees the Dart splash screen. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
