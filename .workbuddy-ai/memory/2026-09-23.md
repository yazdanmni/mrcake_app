- در هدر صفحه اصلی، بخش «ورود / ثبت نام» با پس‌زمینه `AppColors.field`، حاشیه `AppColors.border` با ضخامت ۲ و گوشه‌های گرد ۱۶ تنظیم شد. آواتار کاربر ثبت‌نام‌شده نیز در هدر به‌صورت دایره‌ای با همان حاشیه و ضخامت ۲ حفظ شد.
- HomeHeader now uses one shared `66.r` diameter for avatar width and height so the image stays circular across screen aspect ratios. The greeting uses only trimmed `firstName` in `AppColors.textSecondary`; missing names leave only «سلام» instead of exposing the full name or phone number. Added widget regression coverage for five viewport sizes, compound first names, profile updates, and missing first names in `test/widget_test.dart`.
- Constrained the home greeting with Expanded/Flexible and single-line ellipsis so long names do not overflow or compress the square avatar. Retained guest login button styling and added narrow-screen/guest coverage.
